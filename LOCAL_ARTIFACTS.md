# Local Research Artifacts

Large trained models, datasets, and generated outputs are intentionally excluded from the lightweight public source tree.

Set the relevant environment variables when running pipelines that require external artifacts. See `docs/REPRODUCIBILITY.md`.

The original research repository used Git LFS for large artifacts. This source distribution does not silently download or install those artifacts.
